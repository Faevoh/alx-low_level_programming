#include "main.h"

/**
  * add - Adds two integers
  * @a: first inputed value
  * @b: second inputed value
  *
  * Return: The result after intergers are added
  */

int add(int a, int b)
{
	int sum = (a + b);

	return (sum);
}
